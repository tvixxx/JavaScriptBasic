/* find min number from two numbers */

function minNumb(a,b) {
	if (a > b) {
		return true; //  if need to see our variable "a" write - return a;
	} else {
		return false; //  if need to see our variable "b" write - return b;
	};
};
console.log(minNumb(5,4));
